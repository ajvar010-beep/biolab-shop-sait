# Render.com Blueprint
# Автоматический деплой Biolab

services:
  - type: web
    name: biolab-shop-sait
    env: node
    region: frankfurt
    autoDeploy: true
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 10000
      - key: ALLOWED_ORIGINS
        value: https://biolab-shop-sait.onrender.com
      - key: ADMIN_USERNAME
        value: admin
      - key: ADMIN_PASSWORD
        generateValue: true
      # DATABASE_URL задаётся вручную в дашборде (строка подключения Neon).
      # sync:false — не генерируется блюпринтом и не стирается при обновлении.
      # Без него сервер молча уйдёт на эфемерный SQLite и потеряет данные при рестарте.
      - key: DATABASE_URL
        sync: false
